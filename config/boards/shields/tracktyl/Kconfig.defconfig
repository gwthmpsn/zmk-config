if SHIELD_TRACKTYL_LEFT

    config ZMK_KEYBOARD_NAME
        default "Tracktyl (Left)"

endif

if SHIELD_TRACKTYL_RIGHT

    config ZMK_KEYBOARD_NAME
        default "Tracktyl (Right)"

    config ZMK_SPLIT_BLE_ROLE_CENTRAL
        default y
	
	config PMW3389
		bool
		default y

endif

if SHIELD_TRACKTYL_LEFT || SHIELD_TRACKTYL_RIGHT

    config ZMK_SPLIT
        default y
        
    config ZMK_USB
        default y
endif
